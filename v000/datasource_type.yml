databaseChangeLog:
  - changeSet:
      id: create_datasource_type
      author: thierry_onkelinx
      changes:
        - createTable:
          - tableName: "datasource_type"
          - schemaName: "public"
          - columns:
            - column:
                name: id
                type: smallserial
                constraints:
                  primaryKey: true
                  nullable: false
            - column:
                name: description
                type: varchar(255)
                constraints:
                  unique: true
                  nullable: false

        - createTable:
          - tableName: "datasource_type"
          - schemaName: "staging"
          - columns:
            - column:
                name: description
                type: varchar(255)
