databaseChangeLog:
  - changeSet:
      id: create_scheme
      author: thierry_onkelinx
      changes:
        - createTable:
          - tableName: "scheme"
          - schemaName: "public"
          - columns:
            - column:
                name: fingerprint
                type: varchar(40)
                constraints:
                  primaryKey: true
                  nullable: false
            - column:
                name: description
                type: varchar(255)
                constraints:
                  nullable: false
