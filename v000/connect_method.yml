databaseChangeLog:
  - changeSet:
      id: create_connect_method
      author: thierry_onkelinx
      changes:
        - createTable:
          - tableName: "connect_method"
          - schemaName: "public"
          - columns:
            - column:
                name: fingerprint
                type: varchar(40)
                constraints:
                  primaryKey: true
                  nullable: false
            - column:
                name: description
                type: varchar(255)
                constraints:
                  nullable: false
